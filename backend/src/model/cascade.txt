CREATE TABLE products (
    id PRIMARY KEY,
    name UUID REFERENCES receipts(id) ON DELETE CASCADE,
    product_id INT,
    quantity INT
);
ALTER TABLE products
DROP CONSTRAINT product_id_fkey,
ADD CONSTRAINT product_id_fkey
    FOREIGN KEY (id)
    REFERENCES receipts(id)
    ON DELETE CASCADE;
